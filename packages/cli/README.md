# @falinks/cli

```bash
# 查看版本
npx @falinks/cli -v

# 创建一个名称为 demo 的 vue 组件库
npx @falinks/cli create demo

# 自动生成 .gitignore
# 目前支持的类型：
# vue, vuejs, react, reactnative, node, dart
npx @falinks/cli gi react
```